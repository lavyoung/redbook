spring:
  application:
    name: redbook-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        enabled: true
        server-addr: 192.168.23.200:8848
        namespace: dev
        group: DEFAULT_GROUP
        username: nacos
        password: nacos

